#!/usr/bin/env bash

set -euo pipefail

# Install Python dependencies
pip3 install requests
#IFS=',' read -r -a secret_keys <<< "${SECRET_KEYS}"
# Run the Python script to retrieve secrets
python3 ${BUILDKITE_PLUGIN_DIRECTORY}/get_secrets_azure.py "${SECRET_KEYS}" > secrets.env
#python3 get_secrets_azure.py "Muntashir,work" > secrets.env
source secrets.env